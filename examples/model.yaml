# dataset operations
dataset:
    split:
        test_size: 0.2
        shuffle: True
        stratify: False
    preprocess:
        missing_values: mean
# model definition
model:
    type: classification
    algorithm: random forest

# target you want to predict
target:
    - sick
