model:
    type: ImageClassification
    arguments:
        max_trials: 1

training:
    epochs: 10

dataset:
  source_type: sql
  connection_string: "sqlite:///mydb.sqlite"
  sql_query: "SELECT * FROM my_table"
